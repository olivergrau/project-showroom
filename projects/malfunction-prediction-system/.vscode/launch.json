{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Python: Debug Current File",
          "type": "python",
          "request": "launch",
          "program": "${file}",  // Executes the currently open file
          "console": "integratedTerminal",  // Debug output appears in the terminal
          "env": {
              "PYTHONPATH": "${workspaceFolder}"  // Ensures your workspace is in the Python path
          },
          "python": "/home/oliver/anaconda3/envs/pytorch_env/bin/python", // Path to Conda's Python binary
          "args": []  // Additional command-line arguments, if needed
      }
  ]
}